#include<vector>
#include"complexx.h"
#pragma once
std::vector<Complexx> iter_ftt(const std::vector<Complexx> &a, int n);
void bit_rev_copy(const std::vector<Complexx> &a, std::vector<Complexx> &A, int n, int pow);
